# Autumn fruit, and the bare-earth failure state — 2026-09-20

Both through `prototype/wired.html` (the product's `mountTree` path).

**`SHEET-autumn-fruit.png`** — autumn carries NO flowers; fruit is its carrier.
A1 ikea's real DNA (no fruit trait → no accent: the flagged hole). A2 the same tree IF it
fruited, in ikea's own yellow `#f5d30a` — the hardest case, a warm accent in a warm crown.
A3 red, A4 blue. Compare `../ground-value-2026-09-20/SHEET-few-floor.png`, where the same
yellow as BLOOM did not arrive at 6 or at 15 clusters.

**`SHEET-bare-earth.png`** — E1 the FAILURE state (`?earth=1`). E2 a SUCCESSFUL bare tree
(info.cern.ch), which it must never resemble. E3 failure then a retry that succeeds
(`?earth=1&then=gov.uk`). E4 a tree then a failure (`?site=gov.uk&thenEarth=1`).
